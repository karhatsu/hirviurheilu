- content_for :title, "#{@race.name} - #{clubs_title(@race)}"
%h2#clubs_title= clubs_title(@race)
- club_word = (@race.club_level == Race::CLUB_LEVEL_PIIRI ? 'piirejä' : 'seuroja')
.message.message--info= t '.info', club_word: club_word
.row
  - @race.clubs.each do |club|
    = render partial: 'club_row', locals: { club: club }
.buttons
  = link_to "#{t :add} #{club_title(@race).downcase}", new_official_race_club_path(@race), class: 'button button--add'
.buttons.buttons--nav
  = link_to t(:back_to_official_race_page), official_race_path(@race), class: 'button button--back'

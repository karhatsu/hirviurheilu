%h2= @race.sport.one_batch_list? ? t('.title.one_batch_list') : t('.title.other')
- pdf_url = race_qualification_round_batches_path(@race, format: :pdf)
- if @race.hide_qualification_round_batches
  .message.message--info= t(@race.sport.one_batch_list? ? '.hidden_one_batch_list' : '.hidden')
- else
  = render partial: 'batches/batches', locals: { track_place_name: :qualification_round_track_place, pdf_url: pdf_url }
.buttons.buttons--nav
  = link_to t(:back_to_race_home_page), race_path(@race), class: 'button button--back'
  = link_to t(:download_batch_list_pdf), pdf_url, class: 'button button--pdf'

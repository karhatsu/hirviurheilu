<% content_for(:title, 'Muokkaa omia tietoja') %>
<% content_for(:page_title, yield(:title)) %>

<%= form_for @user, :url => account_path do |f| %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Päivitä" %>
<% end %>

<br/><%= link_to "Takaisin omiin tietoihin", account_path %>
- content_for(:title, t('.title'))
- content_for(:page_title, yield(:title))
= flash_error
= flash_success
%h1.mobile= yield :page_title
- if ProductionEnvironment.staging?
  .warning
    Käytät parhaillaan Hirviurheilun testiympäristöä.
    Varsinaiseen Hirviurheilu-palveluun luomasi tunnukset eivät käy suoraan tähän palveluun.
    Voit luoda testipalveluun omat tunnukset klikkaamalla alla olevaa rekisteröintilinkkiä.
= form_for @user_session, url: user_session_path, html: { class: 'form form--vertical' } do |f|
  = render partial: 'shared/form_errors', locals: { item: @user_session }
  .form__field
    = f.label :email
    = f.email_field :email, autofocus: true
  .form__field
    = f.label :password
    = f.password_field :password
  .form__field
    = f.label :remember_me
    = f.check_box :remember_me
  .form__buttons
    = f.submit t('.submit_button'), class: 'form__button form__button--primary'
%hr
= link_to t('.forgotten_password'), new_reset_password_path, class: 'secondary_link'
= link_to t('.register'), register_path, class: 'secondary_link'

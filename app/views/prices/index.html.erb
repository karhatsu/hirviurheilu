<% content_for :title, "Hinnat" %>
<% content_for :page_title, yield(:title) %>
<% if Rails.env == 'staging' %>
  <div class="important">Käytät tällä hetkellä testiympäristöä. Sen käyttö on ilmaista.</div>
  <div class="action_box"><a href="<%= PRODUCTION_URL %>/prices">Tutustu hintoihin varsinaisessa
    palvelussa</a></div>
<% else %>
  <h2>Yhden kilpailun hinta</h2>
  <div class="section">
    <table>
     <tr>
      <td>Perushinta</td><td><%= "%.2f" % BasePrice.price %> euroa</td>
     </tr>  
      <% @prices.each do |price| %>
        <tr>
          <td><%= price.min_competitors %>-<%= price.max_competitors %> kilpailijaa</td>
          <td><%= "%.2f" % price.price %> <%= t("euros", :count => price.price) %> / kilpailija</td>
        </tr>
      <% end %>
    </table>
    <p>(Kaikki hinnat sisältävät 23 % arvonlisäveroa.)</p>
    <div class="important">Aloitustarjous: Ensimmäinen kilpailu on ilmainen!</div>
  </div>
  <h2>Joukkuekilpailut, viestit, cup-kilpailut</h2>
  Näistä kilpailumuodoista ei veloiteta erikseen.
  <h2>Hintalaskuri</h2>
  <%= form_tag calculate_price_path, :remote => true do %>
    <%= label_tag :competitors, "Kilpailijoita:" %>
    <%= text_field_tag :competitors, @default_competitors_count, :size => 3 %>
    <%= submit_tag "Laske" %>
  <% end %>
  Hinta yhteensä: <span id="calculated_price"><%= "%.2f" % @price %></span> euroa
  <h2>Isot kilpailut, monta kilpailua kauden aikana tms.</h2>
  <%= link_to 'Ota yhteyttä, niin sovitaan tilanteeseen sopiva hinta.', new_feedback_path %>
  <h2>Hirviurheilu Offline</h2>
  <%= link_to 'Katso Hirviurheilu Offline -tuotteen hinta.', offline_price_path %>
<% end %>
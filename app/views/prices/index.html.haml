- content_for :title, t('prices.title')
- content_for :page_title, yield(:title)
- if ProductionEnvironment.staging?
  .message.message--important Käytät tällä hetkellä testiympäristöä. Sen käyttö on ilmaista.
  %a.button{href: "#{PRODUCTION_URL}/prices"} Tutustu hintoihin varsinaisessa palvelussa
- else
  %h2 Metsästäjäliiton jäsenseurat
  .message.message--important
    Hirviurheilu on Metsästäjäliiton jäsenseurojen vapaassa käytössä ilman erillistä veloitusta
  %h2 Muut käyttäjät
  = link_to 'Ota yhteyttä, niin sovitaan tilanteeseen sopiva hinta.', new_feedback_path
.buttons.buttons--nav
  = link_to t(:back_to_home_page), root_path, class: 'button button--back'

- content_for(:title, "Admin - Kilpailut")
%h2= yield :title
= flash_success
%table
  %tr
    %th Nimi
    %th Pvm
    %th Sijainti
    %th Toimitsijat
    %th &nbsp;
  - @races.each do |race|
    %tr
      %td= link_to truncate(race.name, length: 30), admin_race_path(race)
      %td= race_date_interval(race)
      %td= truncate(race.location, length: 30)
      %td= truncate(race.users.collect { |user| full_name(user) }.join(', '), length: 50)
      %td= link_to 'Video', admin_race_video_path(race)

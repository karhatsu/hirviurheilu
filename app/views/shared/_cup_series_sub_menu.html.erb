<div class="menu sub_sub_menu">
  <ul>
    <% @cup.cup_series.each do |cup_series| %>
      <%= menu_item cup_series.name, cup_cup_series_path(@cup, cup_series.name),
        cup_series.name == @cup_series.name, 18 %>
    <% end %>
  </ul>
</div>
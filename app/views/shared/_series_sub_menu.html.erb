<div class="menu sub_menu sub_menu_right">
  <ul>
    <% race.series.each do |series| %>
      <li><%= menu_item series.name, yield(series), series.id == series_id %></li>
    <% end %>
  </ul>
</div>

<div class="menu sub_menu sub_menu_right">
  <ul>
    <% race.series.each do |series| %>
      <%= menu_item series.name, yield(series),
        series.id == series_id, 18 %>
    <% end %>
  </ul>
</div>
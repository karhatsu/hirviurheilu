<% id = defined?(selected_id) ? selected_id : params[:series_id] %>
<%= form_tag(url, :method => :get) do %>
  <%= select_tag :series_id, options_from_collection_for_select(race.series,
    :id, :name, id) %>
  <%= submit_tag "Vaihda sarja" %>
<% end %>
<%
def locale_link(locale, text)
  link_to image_tag("locale_#{locale}.png", :alt => text, :title => text, :class => 'locale_flag'), "?new_locale=#{locale}",
    :class => 'navigation' unless I18n.locale == locale
end
%>
<%= locale_link :fi, 'Suomeksi' %>
<%= locale_link :sv, 'På svenska' %>
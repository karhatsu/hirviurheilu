<h1><%=@series.name%> - tulokset</h1>
<table>
  <tr>
    <th>&nbsp;</th>
    <th>Nimi</th>
    <th>Piiri / seura</th>
    <th>Pisteet</th>
    <th>Ammunta</th>
    <th>Arviointi</th>
    <th><%= @series.race.sport.run? ? "Juoksu" : "Hiihto" %></th>
  </tr>
<% @series.ordered_competitors.each_with_index do |comp, i| %>
  <tr>
    <td><%= i + 1 %>.</td>
    <td><%= comp.last_name %> <%= comp.first_name %></td>
    <td><%= comp.club.name %>.</td>
    <td><%= comp.points %></td>
    <td><%= comp.shot_points %></td>
    <td><%= comp.estimate_points %></td>
    <td><%= comp.time_points %></td>
  </tr>
<% end %>
</table>
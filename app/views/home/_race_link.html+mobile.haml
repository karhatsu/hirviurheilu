.col-xs-12.col-sm-6.col-md-4.col-lg-3
  %a.card{href: race_path(race)}
    .card-middle
      .card-name= race.name
      .card-sub-results #{date_interval(race.start_date, race.end_date)}, #{race.location}

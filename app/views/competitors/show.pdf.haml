%h1= @competitor.series.race.name
#{@competitor.series.race.location}, #{race_date_interval(@competitor.series.race, false)}
%h2
  #{@competitor.series.name} - #{full_name(@competitor)}
  - if @competitor.age_group
    (#{@competitor.age_group.name})
= render partial: 'result_card'

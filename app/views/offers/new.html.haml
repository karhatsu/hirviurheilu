- content_for :title, t('prices_sub_menu.ask_for_offer')
- content_for :page_title, yield(:title)
.info
  Hirviurheilun perushinta on 1 euro / kilpailija, mutta Hirviurheilu tarjoaa alennuspaketteja
  seuroille, jotka järjestävät useita kilpailuita. Kirjoita alla olevaan lomakkeeseen
  yhteystietosi sekä tiedot tulevista kilpailuista, niin otamme sinuun yhteyttä tarjouksen kera.
= form_tag offers_path, class: 'offer_form' do
  %div
    .form_label= label_tag :name, t('attributes.name')
    .form_field= text_field_tag :name, current_user ? full_name(current_user) : ''
  %div
    .form_label= label_tag :email, t('attributes.email')
    .form_field= text_field_tag :email, current_user ? current_user.email : ''
  %div
    .form_label= label_tag :tel, t(:tel)
    .form_field= text_field_tag :tel
  %div
    .form_label= label_tag :club, t(:club)
    .form_field= text_field_tag :club
  %div
    .form_label= label_tag :competition_info, t(:competition_info)
    .form_field= text_area_tag :competition_info, params[:competition_info], cols: 50, rows: 10
  .submit_button
    = submit_tag t(:send)
